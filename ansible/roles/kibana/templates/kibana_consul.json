{
  "service": {
    "name": "kibana",
    "tags": ["master"],
    "port": 5601,
    "address": "{{ ansible_eth1.ipv4.address }}",
    "id": "kibana{{ ansible_eth1["ipv4"]["address"][10:13] }}",
    "enableTagOverride": false
  }
}
