{
  "service": {
    "name": "elasticsearch",
    "tags": ["master"],
    "port": 80,
    "address": "{{ ansible_eth1.ipv4.address }}",
    "id": "elasticsearch{{ ansible_eth1["ipv4"]["address"][10:13] }}",
    "enableTagOverride": false
  }
}
